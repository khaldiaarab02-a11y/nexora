"use client";
import {useEffect,useState} from "react";import Link from "next/link";import {supabase} from "@/lib/supabase/client";import {useI18n} from "@/i18n/LanguageProvider";
export default function NotificationBell(){const {t}=useI18n();const [open,setOpen]=useState(false);const [items,setItems]=useState<any[]>([]);const [unread,setUnread]=useState(0);async function load(){const {data}=await supabase.auth.getSession();const r=await fetch('/api/notifications',{headers:{Authorization:`Bearer ${data.session?.access_token||''}`}});if(r.ok){const d=await r.json();setItems(d.notifications||[]);setUnread(d.unread||0)}}useEffect(()=>{void load();const id=window.setInterval(load,30000);return()=>window.clearInterval(id)},[]);async function mark(id?:string){const {data}=await supabase.auth.getSession();await fetch('/api/notifications',{method:'PATCH',headers:{Authorization:`Bearer ${data.session?.access_token||''}`,'Content-Type':'application/json'},body:JSON.stringify(id?{id}:{})});await load()}return <div className="relative"><button type="button" onClick={()=>setOpen(v=>!v)} className="relative rounded-xl border border-zinc-200 px-3 py-2 text-sm" aria-label={t.notifications.title}>🔔{unread>0&&<span className="absolute -end-1 -top-1 min-w-5 rounded-full bg-red-500 px-1.5 text-center text-[10px] font-bold leading-5 text-white">{unread>99?'99+':unread}</span>}</button>{open&&<div className="absolute end-0 mt-2 w-80 max-w-[calc(100vw-2rem)] rounded-2xl border bg-white p-2 shadow-xl z-50"><div className="flex items-center justify-between px-3 py-2"><b>{t.notifications.title}</b>{unread>0&&<button onClick={()=>mark()} className="text-xs text-zinc-500">{t.notifications.markAllRead}</button>}</div><div className="max-h-96 overflow-auto">{items.length===0?<p className="p-4 text-sm text-zinc-500">{t.notifications.empty}</p>:items.map(n=><Link key={n.id} href={n.href||'#'} onClick={()=>mark(n.id)} className={`block rounded-xl p-3 ${n.read_at?'':'bg-violet-50'}`}><p className="text-sm font-semibold">{n.title}</p><p className="mt-1 text-xs text-zinc-500">{n.body}</p></Link>)}</div></div>}</div>}
