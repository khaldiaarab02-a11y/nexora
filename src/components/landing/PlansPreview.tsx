import Link from "next/link";import {PLAN_LABELS,type PlanId} from "@/config/plans";import {PLAN_PRICING} from "@/config/pricing";
export default function PlansPreview(){return <section id="plans" className="bg-zinc-50"><div className="mx-auto max-w-5xl px-4 py-20 sm:px-6"><div className="text-center"><p className="text-sm font-bold uppercase tracking-widest text-zinc-400">Plans</p><h2 className="mt-3 text-3xl font-bold">Starter و Business</h2><p className="mt-3 text-zinc-500">الأسعار مركزية في إعدادات Nexora ولا يتم اختراعها داخل الواجهة.</p></div><div className="mt-10 grid gap-5 md:grid-cols-2">{(["starter","business"] as PlanId[]).map(plan=><article key={plan} className={`rounded-3xl border bg-white p-7 ${plan==="business"?"border-zinc-900":"border-zinc-200"}`}><h3 className="text-2xl font-bold">{PLAN_LABELS[plan]}</h3><p className="mt-3 text-sm leading-6 text-zinc-500">{plan==="starter"?"الأساسيات لتشغيل متجر إلكتروني.":"كل مزايا Starter مع Themes وتخصيصات متقدمة."}</p><p className="mt-5 font-semibold">{PLAN_PRICING[plan].amount===null?"السعر يُحدد من إعدادات Nexora":`${PLAN_PRICING[plan].amount.toLocaleString()} ${PLAN_PRICING[plan].currency}`}</p><Link href="/auth?mode=signup" className="mt-6 inline-flex rounded-xl bg-zinc-900 px-5 py-3 text-sm font-semibold text-white">اختيار الخطة</Link></article>)}</div></div></section>}
