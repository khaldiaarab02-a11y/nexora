import Link from "next/link";import {type PlanId} from "@/config/plans";import {PLAN_PRICING} from "@/config/pricing";import {useI18n} from "@/i18n/LanguageProvider";
export default function PlansPreview(){const {t}=useI18n();return <section id="plans" className="bg-zinc-50"><div className="mx-auto max-w-5xl px-4 py-20 sm:px-6"><div className="text-center"><p className="text-sm font-bold uppercase tracking-widest text-zinc-400">{t.landing.plansLabel}</p><h2 className="mt-3 text-3xl font-bold">{t.landing.plansTitle}</h2><p className="mt-3 text-zinc-500">{t.landing.plansDescription}</p></div><div className="mt-10 grid gap-5 md:grid-cols-2">{(["starter","business"] as PlanId[]).map(plan=>{const pricing=PLAN_PRICING[plan];return <article key={plan} className={`rounded-3xl border bg-white p-7 ${plan==="business"?"border-zinc-900":"border-zinc-200"}`}><h3 className="text-2xl font-bold">{plan==="starter"?t.plans.starter:t.plans.business}</h3><p className="mt-3 text-sm leading-6 text-zinc-500">{plan==="starter"?t.landing.starterDescription:t.landing.businessDescription}</p><p className="mt-5 font-semibold">{pricing.amount===null?t.landing.priceConfigured:`${pricing.amount.toLocaleString()} ${pricing.currency}`}</p><Link href="/auth?mode=signup" className="mt-6 inline-flex rounded-xl bg-zinc-900 px-5 py-3 text-sm font-semibold text-white">{t.landing.choosePlan}</Link></article>})}</div></div></section>}
