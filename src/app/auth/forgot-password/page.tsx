"use client";
import { FormEvent,useState } from "react"; import Link from "next/link"; import { supabase } from "@/lib/supabase/client"; import { useI18n } from "@/i18n/LanguageProvider";
export default function ForgotPassword(){const {t,dir}=useI18n();const [email,setEmail]=useState("");const [loading,setLoading]=useState(false);const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();setLoading(true);setMessage("");const {error}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/auth/reset-password`});setMessage(error?"تعذر إرسال الرسالة. تحققي من البريد وحاولي مجددًا.":"إذا كان البريد صالحًا، ستصلك رسالة لإعادة تعيين كلمة المرور.");setLoading(false);}return <main dir={dir} className="flex min-h-screen items-center justify-center bg-zinc-50 p-6"><div className="w-full max-w-md rounded-3xl border bg-white p-8"><Link href="/auth" className="text-sm text-zinc-500">← {t.common.back}</Link><h1 className="mt-8 text-3xl font-bold">{t.auth.forgot}</h1><p className="mt-2 text-sm text-zinc-500">أدخلي بريد حسابك وسنرسل رابط الاستعادة.</p><form onSubmit={submit} className="mt-7 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder={t.auth.email} className="w-full rounded-xl border px-4 py-3"/><button disabled={loading} className="w-full rounded-xl bg-zinc-900 px-4 py-3 font-semibold text-white">{loading?t.common.loading:t.auth.continue}</button></form>{message&&<p className="mt-5 rounded-xl bg-zinc-50 p-4 text-sm text-zinc-600">{message}</p>}</div></main>}
