import {NextResponse} from "next/server";import {requireAdmin,serviceClient} from "@/lib/server/auth";
export async function GET(request:Request){try{const g=await requireAdmin(request);if(!g.user)return NextResponse.json({error:"Unauthorized"},{status:401});if(!g.admin)return NextResponse.json({error:"Forbidden"},{status:403});const {data,error}=await serviceClient().from("platform_payment_settings").select("ccp_account,account_holder,payment_methods,instructions,updated_at").eq("id",true).maybeSingle();if(error)throw new Error(error.message);return NextResponse.json({settings:data||null});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Failed"},{status:500});}}
export async function PUT(request:Request){try{const g=await requireAdmin(request);if(!g.user)return NextResponse.json({error:"Unauthorized"},{status:401});if(!g.admin)return NextResponse.json({error:"Forbidden"},{status:403});const b=await request.json();const row={id:true,ccp_account:typeof b.ccp_account==="string"?b.ccp_account.trim():"",account_holder:typeof b.account_holder==="string"?b.account_holder.trim():"",payment_methods:typeof b.payment_methods==="string"?b.payment_methods.trim():"",instructions:typeof b.instructions==="string"?b.instructions.trim():"",updated_at:new Date().toISOString()};const {data,error}=await serviceClient().from("platform_payment_settings").upsert(row,{onConflict:"id"}).select("ccp_account,account_holder,payment_methods,instructions,updated_at").single();if(error)throw new Error(error.message);return NextResponse.json({settings:data});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Failed"},{status:500});}}
